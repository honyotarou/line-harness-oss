{
  "name": "@line-crm/admin-access-proxy-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Same-origin Cloudflare Worker: forwards /api/lh-upstream/* to line-crm Worker with Access service token (see Cloudflare Access CORS docs).",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@line-crm/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250413.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
