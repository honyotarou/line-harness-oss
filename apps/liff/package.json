{
  "name": "liff",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "pnpm --filter @line-crm/shared run build && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@line-crm/shared": "workspace:*",
    "@liff/liff-types": "^2.24.0"
  },
  "devDependencies": {
    "@types/node": "25.5.0",
    "typescript": "^5.7.0",
    "vite": "^6.4.2",
    "vitest": "^2.1.9"
  }
}
